# 自底向上（sink 驱动）

## 适用场景

用户给了可疑操作、危险函数、扫描报告中的 finding，且具有源码访问权限。
**仅适用于代码审计场景**，不适用于黑盒测试。

## 执行步骤

1. 定位可疑的 sink（危险函数/操作点）
2. 反向沿调用链追溯参数来源
3. 判断参数是否来自外部可控源
4. 检查路径上的校验和清洗，确认是否可绕过

## 核心确认点

- 风险点是否来自外部可控
- 校验是否充分、能否绕过
