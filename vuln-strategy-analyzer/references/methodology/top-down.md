# 自顶向下（入口驱动）

## 适用场景

用户给了明确的入口：REST 接口、函数名、路由、Controller 方法等。

## 执行步骤

1. 识别入口的全部输入——参数、header、body 字段、query string、cookie
2. 沿每个输入值正向追踪数据流和变换
3. 标记到达危险 sink 的路径
4. 检查路径上的校验和清洗，确认是否可绕过

## 核心确认点

- 风险点是否来自外部可控
- 校验是否充分、能否绕过
